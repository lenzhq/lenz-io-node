# Upstream snapshot — formatted by Django Ninja's `emit_openapi --pretty`.
# Prettier reformatting would break byte-identity with the Python SDK
# sibling (see CONTRIBUTING.md → OpenAPI snapshot).
openapi.json

# Shared contract fixture — the SAME bytes as the Python SDK's copy. One row
# per line is how it is captured; reformatting would break the parity.
test/fixtures/contract/assess_claims_list.json

# Generated bundle
dist/

# The recorded /review fixtures — the SAME bytes as the Python SDK's copies.
test/fixtures/contract/review_*.json
test/fixtures/contract/error_review_*.json
